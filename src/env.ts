/**
 * Default route root path.
 * For example, if rootPath = '/app', the root path will be '/app', settings page is '/app/settingso', etc.
 */
export const rootPath = import.meta.env.VITE_ROOT_PATH as string;
